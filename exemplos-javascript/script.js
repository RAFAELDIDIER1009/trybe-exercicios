'condição' ? 'retorno de condição' : 'retorno se falsa'

let temperature = 35
let weather = temperature > 30 ? 'tá quente' : 'tá de boa';

console.log(`A temperatura é ${temperature}, portanto, ${weather}.`);